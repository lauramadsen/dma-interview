To run, cd into this directory.  Then:

`npm install`

Then:

`npm start`

In your browser, go to:

http://localhost:8000

This was jumpstarted with the angular seed app: https://github.com/angular/angular-seed